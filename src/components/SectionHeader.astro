---
interface Props {
  eyebrow?: string
  title: string
}

const { eyebrow, title } = Astro.props
---

<header class="section-header">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  <slot />
</header>
